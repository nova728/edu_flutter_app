name: zygc_flutter_prototype
description: >-
  Flutter prototype converted from the high school volunteer advising mobile HTML mockup.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.1.0
  http: ^1.2.1
  uuid: ^4.5.1
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.6.18+6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/images/
